{
  "name": "reilly-asia",
  "compatibility_date": "2025-06-29",
  // Assets serve the site; the Worker only runs first for /infra (proxied
  // handbook) so every other request stays a plain static-asset hit.
  "main": "./worker/index.ts",
  "assets": {
    "directory": "./dist/public",
    "binding": "ASSETS",
    // Unknown paths get dist/public/404.html (the SPA shell, emitted by
    // vite.config.ts) with a real 404 status instead of an empty body.
    "not_found_handling": "404-page",
    "run_worker_first": ["/infra", "/infra/*"]
  },
  "routes": [
    {
      "pattern": "reilly.asia",
      "custom_domain": true
    },
    {
      "pattern": "www.reilly.asia",
      "custom_domain": true
    }
  ]
}
